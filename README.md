Express js swagger autogen with router example

> [!NOTE]
> To start the project you need to run:

#### On root folder
command "node init -y" and "node install"
after "npm run swagger" for start server
